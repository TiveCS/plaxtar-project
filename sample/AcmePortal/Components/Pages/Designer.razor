@page "/designer"
@using Plaxtar.Designer

@* Dev-only host page for the Composer, at a route AcmePortal chooses. Compiled only in
   Debug (Content-removed from Release), so a Release build has no designer route. *@

<Composer />
